{
  // Specify "require" for CommonJS
  "require": "ts-node/register",
  // Specify "loader" for native ESM
  // "loader": "ts-node/esm",
  "extensions": [
    "ts"
  ],
  "spec": [
    "tests/**/*.spec.ts"
  ],
  "watch-files": [
    // "src",
    "tests/**/*.spec.ts"
  ],
  "timeout": 3000
}
